server:
  port: ${SERVER_PORT:9190}
  servlet:
    context-path: ${SERVER_SERVLET_CONTEXT_PATH:/}

spring:
  application:
    name: payment-backend
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  datasource:
    username: ${DB_USERNAME:payment}
    password: ${DB_PASSWORD:payment}
    url: ${DB_URL:jdbc:mysql://localhost:3307/payment?useUnicode=true&characterEncoding=UTF-8}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: false
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6380}
      password: ${REDIS_PASSWORD:}
      database: ${REDIS_DATABASE:0}
  mail:
    host: ${MAIL_HOST:smtp.gmail.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:}
    password: ${MAIL_PASSWORD:}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

security:
  jwt:
    secret: ${JWT_SECRET}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}

internal:
  api:
    key: ${INTERNAL_API_KEY}

bofa:
  fee:
    commercial-fee-rate: ${BOFA_FEE_RATE:0.044}
    fixed-fee-usd: ${BOFA_FIXED_FEE_USD:0.30}
    fx-spread-rate: ${BOFA_FX_SPREAD:0.035}
  checkout:
    client-id: ${BOFA_CLIENT_ID:}
    client-secret: ${BOFA_CLIENT_SECRET:}
    currency: ${BOFA_CURRENCY:USD}
    return-url: ${BOFA_RETURN_URL:http://localhost:${server.port:9190}/api/v1/bofa/checkout/return}
    cancel-url: ${BOFA_CANCEL_URL:http://localhost:${server.port:9190}/api/v1/bofa/checkout/cancel}

springdoc:
  api-docs:
    path: /api-docs
    enabled: ${SWAGGER_ENABLED:true}
  swagger-ui:
    path: /swagger-ui.html
    enabled: ${SWAGGER_ENABLED:true}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}

logging:
  level:
    com.payment.backend: INFO
    org.springframework.security: WARN